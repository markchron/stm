message(STATUS "LaTeX_COMPILER: 	${LATEX_COMPILER}")
message(STATUS "PdfLaTeX_COMPILER: 	${PDFLATEX_COMPILER}")
message(STATUS "BibTeX_COMPILER: 	${BIBTEX_COMPILER}")

include(UseLATEX)
set(LATEX_OUTPUT_PATH ${PROJECT_BINARY_DIR}/doc)
file(GLOB_RECURSE ST_LATEX_LIST 
	RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} content/*.tex
	)
set(PDFNAME "stmain") ## stmain.tex

ADD_LATEX_DOCUMENT(
	${PDFNAME}.tex
	INPUTS ${ST_LATEX_LIST}
	IMAGE_DIRS pictures
	#	BIBFILES library.bib
	USE_GLOSSARY
	DEFAULT_PDF
	)

add_custom_command(TARGET pdf POST_BUILD
	COMMAND ${CMAKE_COMMAND} -E copy
	${CMAKE_CURRENT_BINARY_DIR}/${PDFNAME}.pdf
	${CMAKE_BINARY_DIR}/${PDFNAME}.pdf
	)
